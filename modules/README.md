# modules

First-party reference modules. These exist to exercise the module contract so
it does not rot: install, routing, capability declaration, and a database grant
all get used by real code that ships in this repository.

A module contract with no consumer drifts silently. These are that consumer.
