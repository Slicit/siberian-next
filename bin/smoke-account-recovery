#!/usr/bin/env bash
# Getting back into an account: a reset link by email, read out of the transport
# that received it, and the throttles on both public doors.
set -euo pipefail
cd "$(dirname "$0")/.."
exec sh bin/smoke-account-recovery.sh
