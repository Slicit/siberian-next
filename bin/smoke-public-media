#!/usr/bin/env bash
# Public module media served straight from Storage: no token, no module process
# in the byte path, and no way to reach a private object through the same URL.
set -euo pipefail
cd "$(dirname "$0")/.."
exec sh bin/smoke-public-media.sh
