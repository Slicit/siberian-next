#!/usr/bin/env bash
# A core account resetting its password through the browser, and an app account
# proving it can read the address it signed up with.
set -euo pipefail
cd "$(dirname "$0")/.."
exec sh bin/smoke-core-recovery.sh
