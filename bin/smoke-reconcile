#!/usr/bin/env bash
# Makes the Mobile service forget an installed module, then reconciles and
# checks it came back. Twice, because idempotence is the point.
set -euo pipefail
cd "$(dirname "$0")/.."
exec sh bin/smoke-reconcile.sh
