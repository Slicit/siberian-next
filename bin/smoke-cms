#!/usr/bin/env bash
# The CMS module, both faces: a page of blocks in the browser, the same page as
# JSON for the phone app, and media that resolves through either door.
set -euo pipefail
cd "$(dirname "$0")/.."
exec sh bin/smoke-cms.sh
