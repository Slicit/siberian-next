#!/usr/bin/env bash
# What a module keys its rows by: a name that survives an address changing and
# is never handed to anybody else.
set -euo pipefail
cd "$(dirname "$0")/.."
exec sh bin/smoke-identity.sh
